# The treasury console (web.syn, run from this folder). On the Synsema platform the provision fills all
# of this at creation; locally, `cd client && synsema run vela_client.syn -- devnet` writes the same
# lines for the CLI, and copying them here serves the console.
VELA_RPC_URL=https://devnet.synsema.app/<token>/rpc
VELA_AUTHORITY_URL=https://devnet.synsema.app/<token>/authority
VELA_SUBGRAPH_URL=https://devnet.synsema.app/<token>/subgraph/subgraphs/name/hcce
VELA_PROCESSOR=0xDc64a140Aa3E981100a9becA4E685f962f0cF6C9
VELA_TEE_AUTHENTICATOR=0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0
VELA_TRIGGER_FACTORY=0xC9a43158891282A2B1475592D5719c001986Aaec
VELA_TOKEN=
VELA_MAX_FEE=100
# the owner's signing key (on the devnet, the account that came with your token); the P-521 pair and the agent's key are made by the console when empty
VELA_SECP_KEY=ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
# the admin key for allow-token / allow-authority when it is not the signing key (the devnet hands out Anvil #0; empty on a local kit)
VELA_ADMIN_KEY=
VELA_P521_KEY=
VELA_P521_PUB=
# The standalone agent (agent.syn) reads this same file with ITS OWN keys instead of the owner's, plus:
# AGENT_INTERVAL=30              seconds between looks at inbox/
# SYNSEMA_LLM_PROVIDER=anthropic  free-text invoices need an LLM, and its key (ANTHROPIC_API_KEY=)
